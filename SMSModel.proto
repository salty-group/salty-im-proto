syntax = "proto3";

package com.salty.protos;

option java_multiple_files = true;

enum SMSOperationType {
  UNDEFINED = 0; //未定义，默认值
  REGISTER = 1; //注册
  LOGIN = 2; //登录
  RESET_PASSWORD = 3; //重置密码
}